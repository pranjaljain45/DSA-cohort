// ASCENDING ORDR
import java.util.Arrays;
// under function
Arrays.sort(array name); // normal sorting
Arrays.sort(array name, start idx, end idx);  // fix idx ke liye sorting || end idx is excluded


// DESCENDING ORDER
import java.util.Collections;
// under functions
Arrays.sort(array name, Collections.reverseOrder());
