ARRAY hoti hai but continous store mahi hoti memory me
stroe value in from nodes jisme value aur dusre node ka address jha new val store hoti hai aur next node ka address

Size of LINKEDLIST > ARRAY


DEFINE LINKEDLIST
LinkedList< datatype > list.name = new LinkedList<>();


FUNCTIONS OF LL

size : list.size();

display list : syso(list name);

FROM FISRT
add values  : list.addFirst(val);
top element : list.getFirst();
pop element : list.removeFirst();

FROM LAST 
add values : list.addLast(val);
last element : list.getLast();
pop element : list.removeLast();



ARRAYLIST TO ARRAY
datatype[] arr.name = list.name.toArray(new dtype[0]);
Integer[] array = list.toArray(new Integer[0]);


ARRAY TO ARRAY
list.add(arr.name[i]);









